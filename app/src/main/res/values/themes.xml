<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- I have removed the top Action bar with the theme MaterialComponents.DayNight.NoActionBar -->
    <style name="Theme.BasicQuizApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/btnBackground</item>
        <item name="colorPrimaryVariant">@color/btnBackground</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>

    </style>
    <style name="Leaf">
        <item name="cornerSizeTopLeft" >70%</item>
        <item name="cornerSizeBottomRight" >70%</item>

    </style>
</resources>